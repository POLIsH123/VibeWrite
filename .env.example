# ===========================
# VibeWrite.ai Environment Variables
# ===========================

# Server Configuration
NODE_ENV=development
PORT=3000

# Frontend URL (for CORS and redirects)
FRONTEND_URL=http://localhost:8000

# Stripe Configuration
# Get these from: https://dashboard.stripe.com/apikeys
STRIPE_SECRET_KEY=your_stripe_secret_key_here
STRIPE_PUBLISHABLE_KEY=your_stripe_publishable_key_here

# Stripe Price ID for $10/month subscription
# Create a product and price in Stripe Dashboard, then paste the price ID here
# Example: price_1234567890abcdef
STRIPE_PRICE_ID=your_stripe_price_id_here

# Stripe Webhook Secret (for webhook signature verification)
# Get this when you create a webhook endpoint in Stripe Dashboard
# Example: whsec_1234567890abcdef
STRIPE_WEBHOOK_SECRET=your_stripe_webhook_secret_here

# ===========================
# Setup Instructions:
# ===========================
# 1. Copy this file to .env (do not commit .env to git!)
# 2. Fill in the Stripe API keys and secrets
# 3. NO HUGGING FACE API KEY NEEDED! 🎉
#    The app uses local AI models via @xenova/transformers
#    Models auto-download on first run (may take 1-2 minutes)
# 4. For Stripe:
#    - Sign up at https://stripe.com
#    - Go to Developers > API Keys
#    - Use test keys for development
#    - Create a product for $10/month subscription
#    - Copy the price ID
#    - Set up webhook endpoint pointing to your-domain.com/api/webhook
# 5. Update FRONTEND_URL to your actual frontend URL in production
