{
  "name": "vibewrite-ai",
  "version": "1.0.0",
  "description": "VibeWrite.ai - AI-powered text rewriting with personality",
  "type": "module",
  "scripts": {
    "build": "cd backend && npm install",
    "start": "cd backend && npm start",
    "dev": "cd backend && npm run dev"
  },
  "engines": {
    "node": ">=18.0.0"
  }
}